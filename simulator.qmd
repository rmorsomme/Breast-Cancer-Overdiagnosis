---
title: "simulator"
format: html
editor_options: 
  chunk_output_type: console
---

```{r}
rm(list = ls())
library(tidyverse)
library(tictoc)
source("helpers.R")
```

```{r}
theta <- list(
  rate_H = 0.001 , shape_H = 2,
  rate_P = 0.0015, shape_P = 4,
  beta   = 0.814 , psy     = 0.045
)

prior <- list(
  rate_H = 1, shape_H = 1, # gamma(shape_H, rate_H) prior on Weibull rate for H
  rate_P = 10, shape_P = .01, # gamma(shape_P, rate_P) prior on Weibull rate for P
  a_psy  = 1, b_psy   = 1, # beta(a_psy, b_psy) prior on beta
  a_beta = 1, b_beta  = 1  # beta(a_beta, b_beta) prior on psy
)

age_start <- 40

ages_screen <- seq(age_start, 150, by = 2)
n_screen <- length(ages_screen)

n <- 1e3
d_process <- tibble(
  person_id = numeric(n), age_start = numeric(n), 
  sojourn_H = numeric(n), tau_HP    = numeric(n),
  indolent  = numeric(n),
  sojourn_P = numeric(n), tau_PC    = numeric(n)
  )
d_obs_screen <- tibble(
  person_id  = numeric(), screen_id       = numeric(), 
  age_screen = numeric(), screen_detected = numeric()
)
d_obs_censor <- tibble(
  person_id = numeric(n), censor_type = character(n), censor_time = numeric(n)
  )
```

## Biological process

```{r}
for(i in 1 : n){ # person i
  
  set.seed(i)
  
  # Process
  sojourn_H <- draw_sojourn_H(theta)
  tau_HP    <- age_start + sojourn_H
  indolent  <- rbernoulli(1, theta$psy)
  sojourn_P <- if(!indolent) { draw_sojourn_P(theta) } else { Inf }
  tau_PC    <- tau_HP    + sojourn_P
  
  d_process[i, ] <- tibble(
    i, age_start, sojourn_H, tau_HP, indolent, sojourn_P, tau_PC
    )
}
```

```{r}
# Does Marc find this acceptable?
hist(d_process$tau_HP   , breaks = 100, main = "Healthy to preclinical", xlab = "Age") # 10-15 by age 75

hist(d_process$sojourn_P, breaks = 100, main = "Sojourn time in preclinical", xlab = "Waiting time") # peak at 4-6 years
```

## Observation process -- screens and clinical

```{r}
for(i in 1 : n){  print(i) # person i
  
  set.seed(-i)
  
  tau_HP    <- d_process$tau_HP[i]
  tau_PC    <- d_process$tau_PC[i]
  age_death <- Inf #40 + rexp(1,1/50) 
  
  for(j in 1 : length(ages_screen)){ # screen j
    
    age_screen <- ages_screen[j]
  
    if(min(age_death, tau_PC) < age_screen){
      if(tau_PC < age_death){ # clinical cancer first
        d_obs_censor[i, ] <- tibble(i, "clinical", tau_PC)
      }else if(age_death < tau_PC){ # death first
        d_obs_censor[i, ] <- tibble(i, "censored", age_death)
      }
      break
    }
  
    compartment     <- compute_compartment(age_screen, tau_HP, tau_PC)
    screen_detected <- screen_result(compartment, theta)
    
    d_obs_screen    <- d_obs_screen %>% 
      add_row(
        person_id = i, screen_id = j,
        age_screen = age_screen,
        screen_detected = screen_detected
        )
    
    if(screen_detected){ # positive screen first
      d_obs_censor[i, ] <- tibble(i, "screen", age_screen)
      break
    }
    
  }
  
}
```

```{r}
d_process
d_obs_screen
d_obs_censor
```

```{r}
count(d_obs_censor, censor_type)
count(d_process, indolent)
which(d_obs_censor$censor_type == "clinical")
which(d_obs_censor$censor_type == "censored")
which(d_obs_censor$censor_type == "screen")
```

```{r}
id <- 1 # 11 (clinical), 9 (censored), 1 (screen)
d_obs_censor %>% filter(person_id == id)
d_process    %>% filter(person_id == id)
d_obs_screen %>% filter(person_id == id)
```

## Output

```{r, eval=F}

plot(RATE_H, type = "l")
plot(RATE_P, type = "l")
plot(PSY   , type = "l")
plot(BETA  , type = "l")

# Output
x <- seq(0.00001, max(RATE_H, RATE_P, BETA, PSY), 0.0001)
hist(RATE_H, freq=F); abline(v=theta$rate_H, col = "red"); lines(x, dgamma(x, prior$shape_H, prior$rate_H))
hist(RATE_P, freq=F); abline(v=theta$rate_P, col = "red"); lines(x, dgamma(x, prior$shape_P, prior$rate_P))
hist(PSY   , freq=F); abline(v=theta$psy   , col = "red"); lines(x, dbeta(x, prior$a_psy, prior$b_psy))
hist(BETA  , freq=F); abline(v=theta$beta  , col = "red"); lines(x, dbeta(x, prior$a_beta, prior$b_beta))

coda::effectiveSize(RATE_H)
coda::effectiveSize(RATE_P)
coda::effectiveSize(PSY)
coda::effectiveSize(BETA)

runtime <- runtime$toc - runtime$tic
coda::effectiveSize(RATE_H)/runtime
coda::effectiveSize(RATE_P)/runtime
coda::effectiveSize(PSY)/runtime
coda::effectiveSize(BETA)/runtime

acf(RATE_H)
acf(RATE_P)
acf(PSY)
acf(BETA)

```
